name: serial_com
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  usb_serial: ^0.5.2 # for android
  # serial_communication: ^0.0.2
  flutter_libserialport: ^0.4.0 # For windows
  # libserialport: ^0.3.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
